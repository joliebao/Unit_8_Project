import java.util.Arrays;

public class turnCount {
    // count the amount of turns each path has, put that method in the maze solver
    // other method is to set a checkpoint at a specific point and get it back there
    private String[][] maze;
    public turnCount (String[][] m){
        maze = m;
    }
}
